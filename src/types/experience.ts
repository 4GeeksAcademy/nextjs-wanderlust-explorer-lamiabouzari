export interface Experience {
  id: number;
  title: string;
  category: string;
  destination: string;
  durationDays: number;
  price: number;
  rating: number;
  summary: string;
  description: string;
  season: string;
  groupSize: string;
  highlights: string[];
}
